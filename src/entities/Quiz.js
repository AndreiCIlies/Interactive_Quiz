class Quiz {
    constructor(quizId, quizName, quizQuestions) {
        this.quizId = quizId;
        this.quizName = quizName;
        this.quizQuestions = quizQuestions;
    }
}

export default Quiz;